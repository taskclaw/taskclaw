"use client";

// This component is intentionally empty for now.
// The TaskClaw landing page does not use an About section.
const AboutSection = () => {
  return null;
};

export default AboutSection;
